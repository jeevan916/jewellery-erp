# Jewellery ERP Backend

Django + Django REST Framework backend for the online-only Jewellery ERP.

## Stack

- Python 3.12+
- Django
- Django REST Framework
- MySQL / InnoDB

The accounting foundation is implemented first. Business modules must use accounting services and must never maintain a second ledger state.
